import { View, Text } from "react-native";
import React from "react";
import { ThemedText } from "@/components/ThemedText";

const index = () => {
  return (
    <View>
      <ThemedText>orders tab</ThemedText>
    </View>
  );
};

export default index;
